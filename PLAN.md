# 📋 План исправлений проекта Task Manager

## Общая информация

- **Дата создания плана**: 2024
- **Всего найдено проблем**: 15+
- **Критических ошибок**: 3
- **Оценка времени**: ~6-8 часов

### 📈 Статистика выполнения

- ✅ **Выполнено**: 13 из 13 основных задач
- ✅ **Все дополнительные задачи выполнены**: type hints и docstrings добавлены

**Прогресс**: **100% всех задач выполнено** ✅

#### По этапам:
- ✅ **Этап 1** (Критические ошибки): 3/3 задач — **100% выполнено**
- ✅ **Этап 2** (Логические ошибки): 2/2 задач — **100% выполнено**
- ✅ **Этап 3** (Опечатки и код-стайл): 4/4 задач — **100% выполнено**
- ✅ **Этап 4** (Рефакторинг): 1/1 задач — **100% выполнено**
- ✅ **Этап 5** (Соответствие правилам): 3/3 задач — **100% выполнено**

---

## 🚨 Этап 1: Критические ошибки (Блокирующие)

**Приоритет: КРИТИЧЕСКИЙ**  
**Время выполнения: ~30 минут**

### 1.1 Исправление ошибки в `task_manager/mixins.py`

- **Файл**: `task_manager/mixins.py:44`
- **Проблема**: Использование несуществующего атрибута `redirect_url`
- **Текущий код**:
  ```python
  return redirect(self.redirect_url)  # ❌ redirect_url не существует
  ```
- **Решение**: Заменить на `self.success_url`
- **Исправленный код**:
  ```python
  return redirect(self.success_url)
  ```
- **Время**: ~5 минут
- **Статус**: ✅ **ВЫПОЛНЕНО**

### 1.2 Исправление ошибки в `task_manager/tasks/filters.py`

- **Файл**: `task_manager/tasks/filters.py:17-19`
- **Проблема**: `self.request` недоступен в `FilterSet`
- **Текущий код**:
  ```python
  def filter_by_self_tasks(self, queryset, author, value):
      if value:
          return queryset.filter(author=self.request.user)  # ❌ self.request недоступен
      return queryset
  ```
- **Решение**: Передать `request` через параметры фильтра или использовать `queryset.request`
- **Вариант 1**: Модифицировать FilterSet для работы с request
- **Вариант 2**: Передать user через параметры фильтрации
- **Время**: ~15 минут
- **Статус**: ✅ **ВЫПОЛНЕНО**

### 1.3 Добавление проверки SECRET_KEY

- **Файл**: `task_manager/settings.py:16`
- **Проблема**: Отсутствие валидации SECRET_KEY может привести к падению приложения
- **Текущий код**:
  ```python
  SECRET_KEY = os.getenv('SECRET_KEY')  # ❌ Может быть None
  ```
- **Решение**: Добавить проверку с понятным сообщением об ошибке
- **Исправленный код**:
  ```python
  SECRET_KEY = os.getenv('SECRET_KEY')
  if not SECRET_KEY:
      raise ValueError('SECRET_KEY не установлен в переменных окружения')
  ```
- **Время**: ~10 минут
- **Статус**: ✅ **ВЫПОЛНЕНО**

---

## 🔧 Этап 2: Логические ошибки (Высокий приоритет)

**Приоритет: ВЫСОКИЙ**  
**Время выполнения: ~25 минут**

### 2.1 Исправление метода `get_full_name()` в модели User

- **Файл**: `task_manager/users/models.py:8-9`
- **Проблема**: Возвращает строку с пробелами при пустых полях (`" "`, `"Имя "`, `" Фамилия"`)
- **Текущий код**:
  ```python
  def get_full_name(self):
      return f'{self.first_name} {self.last_name}'  # ❌ Может быть " " или "Имя "
  ```
- **Решение**: Обработать случаи пустых `first_name` и `last_name`
- **Исправленный код**:
  ```python
  def get_full_name(self) -> str:
      """
      Возвращает полное имя пользователя.

      Объединяет имя и фамилию пользователя. Если одно из полей пустое,
      возвращает только заполненное поле. Если оба поля пустые, возвращает username.

      Returns:
          str: Полное имя пользователя или username, если имя и фамилия пустые.
      """
      parts = [self.first_name, self.last_name]
      full_name = ' '.join(part for part in parts if part).strip()
      return full_name or self.username
  ```
- **Время**: ~10 минут
- **Статус**: ✅ **ВЫПОЛНЕНО**

### 2.2 Исправление проверки в `TaskDeleteView.dispatch`

- **Файл**: `task_manager/tasks/views.py:59-67`
- **Проблема**: Некорректная логика проверки статуса ответа
- **Текущий код**:
  ```python
  def dispatch(self, request, *args, **kwargs):
      response = super().dispatch(request, *args, **kwargs)
      if response.status_code != 302:  # ⚠️ Потенциально проблемная логика
          task = self.get_object()
          if task.author != request.user:
              messages.error(request, _('Only the author can delete the task'))
              return redirect(self.success_url)
      return response
  ```
- **Решение**: Пересмотреть логику проверки прав доступа (проверять до dispatch или использовать другой подход)
- **Время**: ~15 минут
- **Статус**: ✅ **ВЫПОЛНЕНО**

---

## ✏️ Этап 3: Опечатки и код-стайл (Средний приоритет)

**Приоритет: СРЕДНИЙ**  
**Время выполнения: ~10 минут**

### 3.1 Исправление опечатки "Wellcome" → "Welcome"

- **Файл**: `task_manager/context_processors.py:45`
- **Проблема**: Опечатка в тексте приветствия
- **Текущий код**:
  ```python
  'label': _('Wellcome') + ', ' + user.username,  # ❌ Wellcome
  ```
- **Решение**: Заменить на правильное написание
- **Исправленный код**:
  ```python
  'label': _('Welcome') + ', ' + user.username,  # ✅ Welcome
  ```
- **Время**: ~2 минуты
- **Статус**: ✅ **ВЫПОЛНЕНО**

### 3.2 Удаление пустых методов `__init__`

- **Файлы**: 
  - `task_manager/statuses/models.py:10-11`
  - `task_manager/labels/models.py:10-11`
- **Проблема**: Бесполезные переопределения `__init__`, которые ничего не делают
- **Текущий код**:
  ```python
  def __init__(self, *args, **kwargs):
      super().__init__(*args, **kwargs)  # ❌ Ничего не делает
  ```
- **Решение**: Удалить эти методы
- **Время**: ~5 минут
- **Статус**: ✅ **ВЫПОЛНЕНО**

### 3.3 Удаление неиспользуемого атрибута `required_fields`

- **Файл**: `task_manager/tasks/forms.py:18`
- **Проблема**: Атрибут `required_fields` не используется Django формами
- **Текущий код**:
  ```python
  class Meta:
      model = Task
      fields = ('name', 'description', 'status', 'executor', 'labels')
      required_fields = ('name', 'description', 'status')  # ❌ Не используется
  ```
- **Решение**: Удалить строку `required_fields`
- **Время**: ~2 минуты
- **Статус**: ✅ **ВЫПОЛНЕНО**

### 3.4 Исправление версии Python в pyproject.toml

- **Файл**: `pyproject.toml:11`
- **Проблема**: Не соответствует правилам проекта (требуется Python 3.12)
- **Текущий код**:
  ```toml
  python = ">=3.10,<4.0"  # ❌ Должно быть >=3.12
  ```
- **Решение**: Изменить на `">=3.12,<4.0"`
- **Исправленный код**:
  ```toml
  python = ">=3.12,<4.0"
  ```
- **Время**: ~2 минуты
- **Статус**: ✅ **ВЫПОЛНЕНО**

---

## 🔄 Этап 4: Рефакторинг (Средний приоритет)

**Приоритет: СРЕДНИЙ**  
**Время выполнения: ~20 минут**

### 4.1 Унификация обработки ProtectedError

- **Файлы**:
  - `task_manager/statuses/views.py:44-52`
  - `task_manager/labels/views.py:44-52`
- **Проблема**: Дублирование логики обработки `ProtectedError`, которая уже есть в `ProtectedErrorHandlerMixin`
- **Текущий код** (повторяется в обоих файлах):
  ```python
  def post(self, request, *args, **kwargs):
      try:
          response = super().delete(request, *args, **kwargs)
          if response.status_code == 302:
              messages.success(request, self.success_message)
          return response
      except ProtectedError:
          messages.error(request, self.error_message)
          return redirect(self.success_url)
  ```
- **Решение**: 
  1. Сначала исправить `ProtectedErrorHandlerMixin` (Этап 1.1) ✅
  2. Затем использовать миксин в `StatusDeleteView` и `LabelDeleteView` ✅
  3. Удалить дублирующийся код из view ✅
- **Время**: ~20 минут
- **Статус**: ✅ **ВЫПОЛНЕНО**

---

## 📚 Этап 5: Соответствие правилам проекта

**Приоритет: НИЗКИЙ (но обязательный по правилам)**  
**Время выполнения: ~4-6 часов**

### 5.1 Добавление аннотаций типов (Type Hints)

- **Объем**: ~50-80 функций и методов во всех `.py` файлах
- **Требование**: Все функции и методы должны содержать аннотации типов
- **Пример исправления**:
  ```python
  # До
  def index(request):
      return render(request, 'index.html')
  
  # После
  from django.http import HttpRequest, HttpResponse
  
  def index(request: HttpRequest) -> HttpResponse:
      """Главная страница приложения."""
      return render(request, 'index.html')
  ```
- **Подход**: Постепенно, начиная с публичных API и критических функций
- **Файлы для исправления**:
  - `task_manager/views.py`
  - `task_manager/users/models.py`, `users/views.py`, `users/forms.py`
  - `task_manager/statuses/models.py`, `statuses/views.py`, `statuses/forms.py`
  - `task_manager/tasks/models.py`, `tasks/views.py`, `tasks/forms.py`, `tasks/filters.py`
  - `task_manager/labels/models.py`, `labels/views.py`, `labels/forms.py`
  - `task_manager/mixins.py`
  - `task_manager/base_views.py`
  - `task_manager/context_processors.py`
  - `task_manager/rollbar_middleware.py`
- **Время**: ~2-3 часа
- **Статус**: ✅ **ВЫПОЛНЕНО**

### 5.2 Добавление docstrings на русском языке

- **Объем**: Все классы и публичные функции
- **Формат**: Google или NumPy style (по правилам проекта)
- **Требование**: Docstrings должны описывать входные параметры, типы и возвращаемое значение
- **Пример**:
  ```python
  def get_full_name(self) -> str:
      """
      Возвращает полное имя пользователя.
      
      Объединяет имя и фамилию пользователя. Если одно из полей пустое,
      возвращает только заполненное поле. Если оба поля пустые, возвращает username.
      
      Returns:
          str: Полное имя пользователя или username, если имя и фамилия пустые.
      """
  ```
- **Время**: ~2-3 часа
- **Статус**: ✅ **ВЫПОЛНЕНО**

### 5.3 Исправление уровня логирования

- **Файл**: `task_manager/settings.py:193`
- **Проблема**: Уровень логирования всегда `DEBUG`, даже в production
- **Текущий код**:
  ```python
  'loggers': {
      '': {
          'level': 'DEBUG',  # ⚠️ Всегда DEBUG
          'handlers': ['console', 'file']
      }
  }
  ```
- **Решение**: Использовать условную настройку уровня
- **Исправленный код**:
  ```python
  'loggers': {
      '': {
          'level': 'DEBUG' if DEBUG else 'INFO',
          'handlers': ['console', 'file']
      }
  }
  ```
- **Время**: ~10 минут
- **Статус**: ✅ **ВЫПОЛНЕНО**

---

## 📊 Сводная таблица задач

| № | Задача | Приоритет | Время | Статус |
|---|--------|-----------|-------|--------|
| 1.1 | Исправление `redirect_url` → `success_url` в mixins.py | КРИТИЧЕСКИЙ | ~5 мин | ✅ **ВЫПОЛНЕНО** |
| 1.2 | Исправление `self.request` в filters.py | КРИТИЧЕСКИЙ | ~15 мин | ✅ **ВЫПОЛНЕНО** |
| 1.3 | Проверка SECRET_KEY в settings.py | КРИТИЧЕСКИЙ | ~10 мин | ✅ **ВЫПОЛНЕНО** |
| 2.1 | Исправление `get_full_name()` в User | ВЫСОКИЙ | ~10 мин | ✅ **ВЫПОЛНЕНО** |
| 2.2 | Исправление логики в TaskDeleteView | ВЫСОКИЙ | ~15 мин | ✅ **ВЫПОЛНЕНО** |
| 3.1 | Исправление опечатки "Wellcome" | СРЕДНИЙ | ~2 мин | ✅ **ВЫПОЛНЕНО** |
| 3.2 | Удаление пустых `__init__` | СРЕДНИЙ | ~5 мин | ✅ **ВЫПОЛНЕНО** |
| 3.3 | Удаление `required_fields` | СРЕДНИЙ | ~2 мин | ✅ **ВЫПОЛНЕНО** |
| 3.4 | Исправление версии Python | СРЕДНИЙ | ~2 мин | ✅ **ВЫПОЛНЕНО** |
| 4.1 | Унификация обработки ProtectedError | СРЕДНИЙ | ~20 мин | ✅ **ВЫПОЛНЕНО** |
| 5.1 | Добавление type hints | НИЗКИЙ | ~2-3 часа | ✅ **ВЫПОЛНЕНО** |
| 5.2 | Добавление docstrings | НИЗКИЙ | ~2-3 часа | ✅ **ВЫПОЛНЕНО** |
| 5.3 | Исправление уровня логирования | НИЗКИЙ | ~10 мин | ✅ **ВЫПОЛНЕНО** |

**Общее время**: ~6-8 часов

---

## 🎯 Рекомендуемый порядок выполнения

1. ✅ **Этап 1** — Немедленно (блокирующие ошибки)
2. ✅ **Этап 2** — Сегодня (логические ошибки)
3. ✅ **Этап 3** — Сегодня/завтра (быстрые правки)
4. ✅ **Этап 4** — По возможности (рефакторинг)
5. ✅ **Этап 5** — Постепенно (соответствие правилам)

---

## ⚠️ Риски и зависимости

1. **Зависимость 4.1 от 1.1**: Исправление `ProtectedErrorHandlerMixin` (1.1) необходимо перед рефакторингом (4.1)
2. **Изменение FilterSet (1.2)**: Может потребовать обновления тестов
3. **Добавление типов (5.1)**: Может выявить скрытые проблемы совместимости типов
4. **Изменение логики (2.2)**: Требует тщательного тестирования функциональности удаления задач

---

## 🧪 Рекомендации по тестированию

После каждого этапа рекомендуется запускать тесты:

```bash
make test
make lint
```

Особое внимание на тесты:
- Тесты удаления статусов/меток (после исправления 1.1 и 4.1)
- Тесты фильтрации задач (после исправления 1.2)
- Тесты удаления задач (после исправления 2.2)

---

## 📝 Примечания

- Все изменения должны следовать правилам проекта (PEP-8, type hints, docstrings на русском)
- Коммиты должны использовать Conventional Commits
- После исправления каждого этапа делать коммит с соответствующим сообщением

---

## ✅ Дополнительные выполненные доработки

### Рефакторинг миксинов и архитектуры
- ✅ Упрощен `AuthAndProfileOwnershipMixin` — убрана проверка авторизации
- ✅ Убран `BaseLoginRequiredMixin` — используется стандартный `LoginRequiredMixin`
- ✅ Улучшен `ProtectedErrorHandlerMixin` — добавлена проверка наличия `success_url`
- ✅ Все views используют базовые классы `Base*View`

### Настройки и валидация
- ✅ Восстановлены стандартные валидаторы паролей (убрано `min_length: 3`)
- ✅ Исправлен `LOCALE_PATHS` в settings.py для правильного пути к locale

### Шаблоны и структура
- ✅ Переименованы все `index.html` → `list.html` для всех приложений
- ✅ Созданы универсальные шаблоны:
  - `templates/create.html` — для создания объектов
  - `templates/update.html` — для обновления объектов
  - `templates/delete.html` — для удаления объектов
- ✅ Обновлены все views для использования универсальных шаблонов

### Модели
- ✅ Убраны кастомные `delete()` из моделей Status и Label (Django автоматически обрабатывает ProtectedError)

### Views
- ✅ Убран пустой метод `delete()` из `UserDeleteView`
- ✅ Все views используют базовые классы с правильными настройками

---

**Последнее обновление**: 2024 (обновлено после выполнения доработок)

